apiVersion: v1
kind: Service
metadata:
  name: vault-db
spec:
  clusterIP: None
  selector:
    app: vault-db
  ports:
    - port: 5432
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vault-db
spec:
  serviceName: vault-db
  replicas: 1
  selector:
    matchLabels:
      app: vault-db
  template:
    metadata:
      labels:
        app: vault-db
    spec:
      containers:
        - name: vault-db
          image: postgres:13.1-alpine@sha256:16af09e480c49226f8e7cd4d602ab27e59f434eb255d2b45bd0cfe1defb86fd7
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: vaultservice-env
          volumeMounts:
            - name: vault-db-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: vault-db-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 5Gi
