apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vpn-service
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - __INGRESS_HOST__
      secretName: __INGRESS_TLS_SECRET_NAME__
  rules:
    - host: __INGRESS_HOST__
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vpn-service
                port:
                  number: 6040
